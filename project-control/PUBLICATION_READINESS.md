# Publication Readiness

Implemented: revision-aware approved-only publisher; idempotent claims/leases; immutable conflict checks; private authoritative ArcGIS service; four public-safe query-only views; privacy verifier; dashboard bound only to public-safe views; fail-closed production behavior.

Not yet authorized/executed: production OAuth activation; final human scientific review of a provenance-cleared non-test observation; first live end-to-end publication proof; historical migration; any scientific claim based on TEST-014.

Required final proof: mobile → Firebase → validation → human QC → APPROVED → publisher → private ArcGIS authoritative record → public-safe views → dashboard, followed by retry/idempotency verification.

Software verification is not evidence of an environmental finding.
