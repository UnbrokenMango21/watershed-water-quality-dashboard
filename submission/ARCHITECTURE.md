# Architecture

Canonical architecture: `docs/ARCHITECTURE_OVERVIEW.md`.

Native mobile → private Firebase workflow → trusted validation → human QC → approved-only publisher → private ArcGIS authoritative service → public-safe views → public dashboard.
