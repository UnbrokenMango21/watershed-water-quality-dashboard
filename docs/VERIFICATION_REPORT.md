# PA Watershed Watch — release verification

Checked 2026-09-25. This records what was actually verified. Passing software tests do not establish a scientific publication or a finished public release.

| Surface | Result | Evidence and limit |
|---|---|---|
| GitHub finalization | MERGED / PASS | [PR #35](https://github.com/UnbrokenMango21/watershed-water-quality-dashboard/pull/35) passed all nine checks on its final head and merged into integration as `d30d025` on 2026-09-25. The earlier [full CI retry](https://github.com/UnbrokenMango21/watershed-water-quality-dashboard/actions/runs/36096251241) passed all five jobs on attempt 2; attempt 1's Android emulator went offline. |
| GitHub integration | 17/17 CHECKS PASS | [PR #29](https://github.com/UnbrokenMango21/watershed-water-quality-dashboard/pull/29), [PR #40](https://github.com/UnbrokenMango21/watershed-water-quality-dashboard/pull/40), and [PR #41](https://github.com/UnbrokenMango21/watershed-water-quality-dashboard/pull/41) merged into the existing integration branch in that order. Draft [PR #34](https://github.com/UnbrokenMango21/watershed-water-quality-dashboard/pull/34) passed all 17 checks on `7dcac7d`, including iOS, Android, five CodeQL languages, both websites, backend, publication, visual QA, and development App Hosting rollout. The PR stays draft for human and scientific release gates. |
| Local scientific contracts | PASS | `bash scripts/dev.sh contracts`: 30 validation, 15 publication, and 2 provisioning privacy tests. |
| Firebase rules, validation, review, trigger | PASS | `bash scripts/dev.sh emulators`: 42 Firestore-rule, 6 Storage-rule, 7 validation persistence/orchestration, 16 reviewer lifecycle, and 1 Firestore-trigger tests passed. The first local attempt stalled on iCloud placeholder dependencies; reinstalling the existing lockfile dependencies resolved it. |
| QC Console and public dashboard | BUILD PASS / DEVELOPMENT DASHBOARD LIVE | On combined commit `7dcac7d`, both website typechecks and production builds, plus six public adapter tests, passed locally. [PR #41](https://github.com/UnbrokenMango21/watershed-water-quality-dashboard/pull/41) passed its demo and empty browser checks, including the phone warning. App Hosting successfully deployed that exact commit. A fresh visit to the [hosted development dashboard](https://public-dashboard-dev--central-pa-watershed-dev.us-central1.hosted.app/) showed a connected public source, zero sites and watersheds, no ArcGIS error or demo data, and no horizontal overflow at 1440, 390, or 320 px. The phone Sites, Map, Readings, and Time series views loaded. This proves an empty connected source, not a populated scientific publication. |
| Production dependency audit | PASS for known production advisories | `npm audit --omit=dev` in the backend, `web/`, and `public-dashboard/` reports zero vulnerabilities after targeted patched dependencies. The backend's full development-tool audit still reports three moderate advisories in the Firebase CLI's OpenTelemetry/PubSub dependency chain; npm offers no nonbreaking fix. Reassess when that CLI dependency is patched. |
| Firebase development project | CONNECTED / RECOVERY CONFIGURED | On 2026-09-25, Firestore readback confirmed deletion protection, one Sunday backup schedule with 30-day retention, and PITR off. No backup exists yet, so restore has not been proven. The four tracked indexes match the remote indexes. [PR #40](https://github.com/UnbrokenMango21/watershed-water-quality-dashboard/pull/40) merged the disabled-publisher deployment fix; `validateSubmittedObservation` is the only listed function and is Node 22 / ACTIVE. The publisher and its OAuth secrets remain absent; generated function images have a 30-day cleanup policy. There is no end-user Storage bucket because media is deferred. |
| iOS | TESTFLIGHT VERIFIED / device PENDING | A [fresh App Store Connect query](https://github.com/UnbrokenMango21/watershed-water-quality-dashboard/actions/runs/36157613290) reports signed `0.1.0 (13)` as `VALID`, unexpired, and internal `IN_BETA_TESTING`. Physical iPhone install, durable draft, sign-in, and private test submission remain unverified. |
| Android | COMBINED CI PASS | [PR #40](https://github.com/UnbrokenMango21/watershed-water-quality-dashboard/pull/40) passed unit, lint, builds, and emulator instrumentation on retry; its first Android run failed when the GitHub emulator dropped an APK install connection. The combined integration Android job then passed on `7dcac7d`. |
| ArcGIS publication | ANONYMOUS VIEWS PASS / LIVE PROOF PENDING | Fresh anonymous REST checks of all four public-safe views returned HTTP 200, zero records, `Query`-only capabilities, and no protected fields. The private authoritative service returned an authentication error and exposed no layers. Authenticated exact-item verification, item-scoped OAuth, authorized non-test approval, and end-to-end publication remain gates. |
| Repository security alert | PATCHED LOCALLY / SCAN PENDING | The privileged TestFlight workflow no longer accepts issue-comment release commands; it now uses explicit `workflow_dispatch` input. [CodeQL alert #1](https://github.com/UnbrokenMango21/watershed-water-quality-dashboard/security/code-scanning/1) remains open on the default branch until the fix reaches the main-branch scan and its resolution is confirmed. |

The separate GitHub AI code-scanner failure attempted an unavailable model; it is not evidence of a code finding. TEST-014 is controlled development data, and the historical 117-record/5-site inventory has unresolved provenance. Neither may be used as the first public scientific proof.

Next gates, in order: complete physical iPhone and real-reviewer evidence; verify exact ArcGIS item scope and privacy; approve and trace one provenance-cleared non-test observation; then reconcile, test, tag, and publish the final `main` release. See [the active checklist](../project-control/RELEASE_GATES.md).
