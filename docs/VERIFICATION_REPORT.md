# PA Watershed Watch — release verification

Checked 2026-09-25. This records what was actually verified. Passing software tests do not establish a scientific publication or a finished public release.

| Surface | Result | Evidence and limit |
|---|---|---|
| GitHub finalization | MERGED / PASS | [PR #35](https://github.com/UnbrokenMango21/watershed-water-quality-dashboard/pull/35) passed all nine checks on its final head and merged into integration as `d30d025` on 2026-09-25. The earlier [full CI retry](https://github.com/UnbrokenMango21/watershed-water-quality-dashboard/actions/runs/36096251241) passed all five jobs on attempt 2; attempt 1's Android emulator went offline. |
| GitHub integration | CURRENT HEAD RUNNING | Draft [PR #34](https://github.com/UnbrokenMango21/watershed-water-quality-dashboard/pull/34) passed 16/16 checks on `d30d025`. [PR #36](https://github.com/UnbrokenMango21/watershed-water-quality-dashboard/pull/36) merged the dashboard connection fix as `998d3af`, but its first App Hosting rollout failed on a missing standalone route manifest. [PR #39](https://github.com/UnbrokenMango21/watershed-water-quality-dashboard/pull/39) corrected that packaging, passed all eight of its checks, and merged as `adcdd8a`. PR #34 is rerunning on the combined integration line and remains a draft until non-software release gates are satisfied. |
| Local scientific contracts | PASS | `bash scripts/dev.sh contracts`: 30 validation, 15 publication, and 2 provisioning privacy tests. |
| Firebase rules, validation, review, trigger | PASS | `bash scripts/dev.sh emulators`: 42 Firestore-rule, 6 Storage-rule, 7 validation persistence/orchestration, 16 reviewer lifecycle, and 1 Firestore-trigger tests passed. The first local attempt stalled on iCloud placeholder dependencies; reinstalling the existing lockfile dependencies resolved it. |
| QC Console and public dashboard | BUILD PASS / DEVELOPMENT DASHBOARD LIVE | `bash scripts/dev.sh web-checks`: six public adapter tests, both TypeScript checks, and both production builds passed in the reliable local checkout. A live browser review found the old dashboard reported ArcGIS unavailable because the browser rejected an unbound native `fetch` call. [PR #36](https://github.com/UnbrokenMango21/watershed-water-quality-dashboard/pull/36) fixed it and passed build, demo, and empty-view browser checks. The first App Hosting rollout failed after `next build` because the standalone route manifest was missing; [PR #39](https://github.com/UnbrokenMango21/watershed-water-quality-dashboard/pull/39) applies the same standalone/root configuration used by the working QC Console. Its local production build, TypeScript check, six adapter tests, and route-manifest check passed. Firebase CLI reported a successful development rollout of `892a7b2`, and a fresh browser visit to the [hosted development dashboard](https://public-dashboard-dev--central-pa-watershed-dev.us-central1.hosted.app/) showed a connected public source, zero monitoring sites, and no connection error or demo records. This is an empty-source connection check, not a populated scientific publication. |
| Production dependency audit | PASS for known production advisories | `npm audit --omit=dev` in the backend, `web/`, and `public-dashboard/` reports zero vulnerabilities after targeted patched dependencies. The backend's full development-tool audit still reports three moderate advisories in the Firebase CLI's OpenTelemetry/PubSub dependency chain; npm offers no nonbreaking fix. Reassess when that CLI dependency is patched. |
| Firebase development project | CONNECTED / RECOVERY CONFIGURED; VALIDATION REDEPLOY GATED | CLI selected `central-pa-watershed-dev`; the `validateSubmittedObservation` Node 22 function is ACTIVE, and both App Hosting backends are configured from this GitHub repository. Firestore database deletion protection and a weekly Sunday backup schedule with 30-day retention were read back; point-in-time recovery remains disabled. The first scheduled backup has not yet occurred. The tracked JSON now matches all four deployed Firestore indexes without deleting a remote index. The active validation function was last deployed 2026-08-15, before its 2026-08-16 source change. A scoped redeploy was attempted, but the Firebase CLI requires the ArcGIS publisher OAuth secrets even when only validation is selected; no placeholder secrets or publisher deployment were made. There is no end-user Storage bucket because media capture is deferred; Storage rules were tested in emulators only. See [Firebase safeguards](https://github.com/UnbrokenMango21/watershed-water-quality-dashboard/issues/37). These checks are not a reviewer sign-in or publication proof. |
| iOS | CI PASS / device PENDING | CI iOS job passed; signed TestFlight `0.1.0 (13)` is in beta. Physical iPhone install, durable draft, sign-in, and private test submission remain unverified. |
| Android | CI PASS | Android unit, lint, build, and emulator instrumentation completed in the full CI retry. No claim of a new physical Android device test. |
| ArcGIS publication | ANONYMOUS VIEWS PASS / LIVE PROOF PENDING | On 2026-09-25, direct anonymous REST checks of the four public-safe views returned zero records each, Query-only capabilities, and no unexpected private fields. Anonymous access to the authoritative item/service returned 403/499. Approved-only publisher and view contracts are documented in [Phase 12](PHASE12_ARCGIS_PUBLICATION.md). Authenticated exact-item verification, item-scoped OAuth, authorized non-test approval, and end-to-end publication remain gates. |
| Repository security alert | PATCHED LOCALLY / SCAN PENDING | The privileged TestFlight workflow no longer accepts issue-comment release commands; it now uses explicit `workflow_dispatch` input. [CodeQL alert #1](https://github.com/UnbrokenMango21/watershed-water-quality-dashboard/security/code-scanning/1) remains open on the default branch until the fix reaches the main-branch scan and its resolution is confirmed. |

The separate GitHub AI code-scanner failure attempted an unavailable model; it is not evidence of a code finding. TEST-014 is controlled development data, and the historical 117-record/5-site inventory has unresolved provenance. Neither may be used as the first public scientific proof.

Next gates, in order: verify PR #34 checks on the latest integration head; complete physical iPhone and real-reviewer evidence; resolve the validation redeploy without weakening the ArcGIS gate; verify exact ArcGIS item scope and privacy; approve and trace one provenance-cleared non-test observation; then reconcile, test, tag, and publish the final `main` release. See [the active checklist](../project-control/RELEASE_GATES.md).
